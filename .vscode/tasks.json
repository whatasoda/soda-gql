{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build:extension",
      "type": "shell",
      "command": "bun run build && cd packages/vscode-extension && bun run build",
      "group": "build",
      "problemMatcher": []
    }
  ]
}
