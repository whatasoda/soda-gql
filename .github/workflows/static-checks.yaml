name: Static Checks

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  quality-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76 # v2
        with:
          bun-version-file: .bun-version

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Prepare quality checks
        run: bun quality:prepare

      - name: Run quality checks
        run: bun quality

  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76 # v2
        with:
          bun-version-file: .bun-version

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Prepare for tests
        run: bun quality:prepare

      - name: Run tests with coverage
        run: bun run test --coverage

      - name: Generate coverage summary
        if: github.event_name == 'pull_request'
        run: bun scripts/generate-coverage-summary.ts

      - name: Comment coverage on PR
        if: github.event_name == 'pull_request'
        uses: marocchino/sticky-pull-request-comment@773744901bac0e8cbb5a0dc842800d45e9b2b405 # v2
        with:
          path: coverage-summary.md

  # NOTE: swc-tests disabled to reduce GitHub Actions usage.
  # Rust compilation is expensive. Run locally before release.
  # swc-tests:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
  #
  #     - name: Setup Bun
  #       uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76 # v2
  #       with:
  #         bun-version-file: .bun-version
  #
  #     - name: Setup Rust
  #       uses: dtolnay/rust-toolchain@stable
  #
  #     - name: Setup Rust cache
  #       uses: Swatinem/rust-cache@v2
  #       with:
  #         workspaces: "packages/swc -> target"
  #         cache-targets: "true"
  #
  #     - name: Install dependencies
  #       run: bun install --frozen-lockfile
  #
  #     - name: Prepare for tests
  #       run: bun quality:prepare
  #
  #     - name: Build swc native module
  #       working-directory: packages/swc
  #       run: bun run build
  #
  #     - name: Run swc tests
  #       run: bun run test --filter swc
