table:
  name: page_templates
  schema: public
object_relationships:
  - name: site
    using:
      foreign_key_constraint_on: site_id
array_relationships:
  - name: pages
    using:
      foreign_key_constraint_on:
        column: template_id
        table:
          name: pages
          schema: public
