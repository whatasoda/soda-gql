schema {
  query: Query
}

type Query {
  user(id: ID!): User
  users(limit: Int): [User!]!
  searchUsers(filter: UserFilter!): [User!]!
}

input UserFilter {
  name: String!
  minAge: Int
}

type User {
  id: ID!
  name: String!
  email: String
}
