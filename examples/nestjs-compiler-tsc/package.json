{
  "name": "@soda-gql-examples/nestjs-compiler-tsc",
  "version": "0.0.1",
  "private": true,
  "description": "NestJS example using TypeScript compiler plugin for soda-gql",
  "scripts": {
    "codegen": "soda-gql codegen",
    "artifact": "soda-gql builder --mode zero-runtime --entry ./src/**/*.ts --out ./.cache/soda-gql-artifact.json",
    "build": "nest build",
    "dev": "nest start --watch",
    "start": "node dist/main.js",
    "start:dev": "nest start --watch",
    "start:debug": "nest start --debug --watch",
    "start:prod": "node dist/main"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/platform-express": "^10.0.0",
    "@soda-gql/core": "workspace:*",
    "@soda-gql/runtime": "workspace:*",
    "reflect-metadata": "^0.2.0",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.0.0",
    "@nestjs/schematics": "^10.0.0",
    "@soda-gql/cli": "workspace:*",
    "@soda-gql/tsc-plugin": "workspace:*",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
